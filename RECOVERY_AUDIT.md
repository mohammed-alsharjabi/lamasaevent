# تقرير تدقيق واستعادة موقع لمسه التميز

تاريخ التدقيق: 2026-07-25  
النطاق المرجعي: `https://lams-event.com`  
مصدر القراءة فقط: `/Users/mohammedalsharjabi/Desktop/public_html`

> تعامل هذه الخطة مجلد `public_html` المرفق باعتباره `legacy-dist`. لا تتطلب
> الاستعادة نقله أو إعادة تسميته أو تعديل أي ملف داخله. بصمة ملف
> `sitemap.xml` الأصلية هي
> `65765fca188fcb24f884163a785b97343ad273485c2c8efe7158411541c95130`.

## 1. الملخص التنفيذي

النسخة القديمة هي Production Build ثابت لمشروع Astro، وليست مشروع مصدر قابلًا
للصيانة. تحتوي على HTML مصغر، حزمة CSS مشتركة، ثلاث حزم JavaScript صغيرة،
وصور الأعمال. خريطة الموقع هي عقد التوافق الأساسي: تضم 156 رابطًا يجب أن
يبقى مسار كل منها وCanonical الخاص به دون تغيير.

ستتم الاستعادة في مشروع مستقل يتكون من:

- Laravel: قاعدة البيانات، API، المصادقة، لوحة التحكم، إدارة المحتوى،
  redirects، رفع الصور، وتوليد sitemap.
- Astro: واجهة عامة مطابقة للنسخة القديمة، تبنى من بيانات منظمة ومكونات
  مشتركة.
- Importer قابل لإعادة التشغيل: يقرأ النسخة القديمة فقط، يستخرج المحتوى
  والـSEO والصور، ويكتب إلى قاعدة البيانات الجديدة.
- اختبارات قبول تعتمد على `route-manifest.json` وتقارن المسارات والـSEO
  والروابط الداخلية والاستجابة البصرية.

## 2. بنية النسخة القديمة

| الجزء | الوصف |
|---|---|
| `index.html` | الصفحة الرئيسية |
| `about/`, `contact/`, `gallery/` | صفحات ثابتة |
| `services/` | فهرس الخدمات، 10 تصنيفات، و43 صفحة خدمة |
| `areas/` | فهرس المناطق و5 صفحات مناطق الرياض |
| `blog/` | فهرس المدونة و91 مقالًا |
| `_astro/` | ملف CSS مشترك واحد وثلاث حزم JavaScript مبنية |
| `assets/media/` | 11 مجموعة صور أعمال |
| `icons/` | الشعار، الأيقونات، وصورة المشاركة |
| `sitemap.xml` | 156 عنوان URL؛ مرجع المسارات الملزم |
| `robots.txt` | تعليمات محركات البحث الحالية |
| `404.html` | صفحة الخطأ؛ ليست ضمن sitemap |
| `google93ac6571a02c8d53.html` | ملف إثبات ملكية Google |

إجمالي النسخة: 269 ملفًا بحجم يقارب 26MB. الأنواع: 158 HTML، 97 صورة
(88 JPEG، 2 JPG، 7 PNG)، 3 JavaScript، 1 CSS، 1 XML، و2 TXT. يوجد 157
صفحة HTML مكتملة بعناصر العنوان والوصف وOpen Graph وJSON-LD والهيدر والفوتر؛
ملف إثبات Google هو الاستثناء المقصود. جميع روابط sitemap البالغ عددها 156
لها Canonical و`og:url`.

### تصنيف صفحات sitemap

| النوع | العدد |
|---|---:|
| الصفحة الرئيسية | 1 |
| صفحات الفهارس/الثابتة | 6 |
| تصنيفات الخدمات | 10 |
| الخدمات | 43 |
| المناطق | 5 |
| المقالات | 91 |
| **الإجمالي** | **156** |

## 3. نموذج البيانات المقترح

### جداول المحتوى

- `articles`: العنوان، slug، الملخص، الصورة، محتوى منظم إلى blocks، التصنيف،
  المسودة/المنشور، وتاريخ النشر.
- `service_categories`: الاسم، slug، الوصف، الترتيب، والـSEO.
- `services`: العنوان، slug، الملخص، blocks، التصنيف، الصور، الحالة، وتاريخ
  النشر.
- `areas`: الاسم، slug، الوصف، blocks، الحالة، وتاريخ النشر.
- `pages`: الصفحات الثابتة والفهارس مع نوع الصفحة وblocks منظمة.
- `media`: الملف الأصلي، نسخة WebP، MIME، الأبعاد، الحجم، alt، caption،
  checksum، وترتيب العرض.
- `galleries` و`gallery_media`: مجموعات الصور وعلاقتها بالوسائط.

### جداول التشغيل والـSEO

- `seo_meta`: علاقة polymorphic تشمل title، description، canonical،
  Open Graph، Twitter، robots، hreflang، وSchema JSON-LD.
- `route_registry`: المسار الحالي، النوع، معرّف الكيان، هل هو منشور،
  ومرجع sitemap. المسار المستورد والمنشور لا يعدل مباشرة.
- `redirects`: `from_path` فريد، `to_path`، status=301، السبب، ومن أنشأه.
- `sitemap_entries`: loc الأصلي، lastmod، changefreq، priority، الترتيب،
  وحالة التضمين.
- `contact_settings`: الهاتف، WhatsApp، البريد، العنوان، وحسابات التواصل.
- `site_settings`: الهوية النصية والإعدادات العامة وSEO الافتراضي.
- `users`: مستخدمو الإدارة بكلمات مرور hashed.
- `activity_logs`: أثر تدقيقي لتغييرات الإدارة الحساسة.

### سياسة slug

قبل أول نشر يمكن تعديل slug. بعد النشر لا يتم تغييره إلا من خلال عملية
ذرية تنشئ Redirect 301 من المسار القديم إلى الجديد، تتحقق من عدم وجود تعارض،
ثم تحدث `route_registry`. يمنع Observer/Service أي تحديث مباشر يتجاوز هذه
العملية.

## 4. استراتيجية الحفاظ على الروابط وSEO

1. `route-manifest.json` مولد مباشرة من `legacy-dist/sitemap.xml` ويحتفظ
   بالعنوان الكامل، المسار بما فيه trailing slash، خصائص sitemap، ملف HTML
   المقابل، بصمته، العنوان، الوصف، Canonical، Open Graph، Twitter، JSON-LD،
   والروابط الداخلية.
2. يعد الـManifest عقد قبول غير قابل للتعديل اليدوي؛ يعاد توليده فقط من
   المرجع القديم، وتتحقق الاختبارات من بصمة sitemap الأصلية.
3. Astro يولد كل مسار في الـManifest، مع إبقاء Canonical و`og:url` على
   `https://lams-event.com` حتى على Staging، ويمنع فهرسة Staging عبر
   `X-Robots-Tag`/`robots.txt` خاص بالبيئة دون تغيير metadata الإنتاج
   الموروثة.
4. يحتفظ Laravel بالقيم الأصلية في سجلات SEO وsitemap. أي صفحة جديدة لا
   تزاحم مسارًا موروثًا.
5. الروابط الداخلية تبقى كما هي حرفيًا. تختبر الاستجابات، Canonical،
   metadata، Schema، وعدم وجود روابط داخلية مكسورة.
6. تحفظ ملفات التحقق والأيقونات ومسارات الصور القديمة. أي تحسين WebP يضاف
   كنسخة مشتقة مع بقاء الأصل والمسار القديم متاحين.

## 5. خطة الاستعادة المرحلية

1. **التدقيق والعقد:** تثبيت هذا التقرير، Route Manifest، وبصمات المرجع.
2. **النواة الخلفية:** Laravel، قاعدة البيانات، المهاجرات، API، مصادقة
   الإدارة، CRUD، سجل النشاط، وسياسة تغيير slug.
3. **الاستيراد والوسائط:** Parser للـHTML/SEO/JSON-LD، إدخال البيانات
   المنظمة، نسخ الصور إلى تخزين التطبيق، فحص MIME والحجم، وتوليد WebP.
4. **الواجهة:** Astro RTL، مكونات Header/Footer/Booking/SEO مشتركة، routes
   ديناميكية لكل نوع محتوى، والحفاظ على CSS والهوية والمحتوى.
5. **القبول:** اختبارات Laravel/Astro، فحص كل روابط الـManifest، مقارنة
   metadata والروابط الداخلية، وPlaywright screenshots للقديم والجديد.
6. **Staging:** قاعدة بيانات ووسائط مستقلة، حماية وصول، `noindex` على مستوى
   الخادم، تشغيل migrations/import/build والاختبارات، ثم مراجعة بشرية.
7. **الإطلاق لاحقًا:** لا يتم ربط الدومين الرئيسي إلا بعد اعتماد تقرير
   المقارنة ونسخة احتياطية وخطة رجوع مجربة.

كل مرحلة تنفذ في Commit مستقل يمكن مراجعته أو الرجوع عنه دون خلط المراحل.

## 6. المخاطر وإجراءات الحد منها

| الخطر | الأثر | الإجراء |
|---|---|---|
| HTML مبني ومصغر ولا يوجد source | فقد دلالات المكونات الأصلية | استخراج typed entities وcontent blocks مع حفظ HTML الدلالي اللازم للتحقق |
| اختلاف trailing slash | Redirect أو duplicate content | استخدام المسار الحرفي من الـManifest في البناء والاختبار |
| فقد metadata أو JSON-LD أثناء التحرير | تراجع SEO | حقول SEO مستقلة واختبارات snapshot لكل route |
| اختلاف الصور أو الخطوط | فشل المقارنة البصرية وCLS | حفظ المسارات والأبعاد ونسخ الأصل، وإضافة WebP كمشتق |
| تغيير slug بالخطأ | 404 وخسارة ترتيب | قفل slug بعد النشر وRedirect 301 ذري |
| Staging قابل للفهرسة | محتوى مكرر | نطاق منفصل، حماية وصول، و`X-Robots-Tag: noindex` |
| Parser لا يغطي صفحة شاذة | محتوى ناقص | تقرير import anomalies وفشل صريح بدل التجاوز الصامت |
| رفع ملف ضار | اختراق لوحة التحكم | allowlist MIME، فحص الحجم/الأبعاد، اسم عشوائي، تخزين خارج public، وإعادة ترميز |
| تباين بيئة Laravel/Astro | فشل النشر | إعداد Staging قابل لإعادة البناء وفحوص health/ready |

## 7. خطوات الرجوع إلى النسخة السابقة

1. لا يحذف أو يستبدل `legacy-dist` في أي مرحلة، ولا يكتب إلى مجلد
   `public_html` المرجعي.
2. قبل الإطلاق المستقبلي تؤخذ نسخة احتياطية من قاعدة البيانات والوسائط
   وإعدادات الخادم، وتسجل نسخة التطبيق الحالية.
3. يتم خفض DNS TTL قبل نافذة الإطلاق عند الحاجة، مع إبقاء نسخة الاستضافة
   القديمة قابلة للخدمة.
4. عند فشل التحقق بعد الإطلاق: يعاد توجيه upstream/virtual host فورًا إلى
   النسخة القديمة أو يعاد نشر artifact القديم، دون تشغيل redirects الجديدة.
5. تستعاد قاعدة البيانات والوسائط فقط إذا حدثت كتابات إنتاجية لا تتوافق مع
   النسخة المرجوع إليها.
6. يعاد تشغيل فحص أهم المسارات وملف sitemap وملفات Google verification، ثم
   يراقب سجل 404 و5xx.

## 8. شروط القبول

- تطابق مجموعة روابط sitemap الجديدة مع الـ156 رابطًا الأصلي دون زيادة أو
  نقص أو تغيير.
- استجابة 200 لكل رابط موروث (أو 301 موثق فقط عند تغيير slug معتمد لاحقًا).
- تطابق title، description، Canonical، Open Graph، Twitter، JSON-LD،
  والروابط الداخلية مع المرجع.
- عدم وجود أي تغيير في بصمات ملفات `legacy-dist`.
- نجاح اختبارات Laravel وAstro وPlaywright، ومراجعة فروق screenshots ضمن
  الحدود المعلنة.
- اعتماد Staging قبل أي إجراء على الدومين الرئيسي.
