# دليل Staging والرجوع

## حدود هذه المرحلة

- النسخة القديمة في `LEGACY_DIST` مرجع ثابت ولا تدخل ضمن ملفات النشر الجديدة.
- لا يوجد في المشروع أمر يغيّر DNS أو إعداد الدومين الرئيسي.
- Staging تستخدم قاعدة بيانات وCookie مستقلين وترسل `noindex`.
- النشر العام الكامل يحتاج استضافة تدعم PHP 8.2+ وقاعدة بيانات وTLS ومجلد
  تخزين دائم؛ لذلك لا يكفي نشر ملفات Astro وحدها للقبول التشغيلي للوحة.

## بوابة القبول قبل أي تحويل

1. أخذ Snapshot من قاعدة Staging ومجلد `backend/storage/app/public`.
2. تشغيل `scripts/staging-prepare.sh` على نسخة قاعدة منفصلة.
3. التأكد من نجاح استيراد 156 مسارًا وبصمات المرجع.
4. تشغيل اختبارات Laravel وAstro وعقد المسارات وPlaywright.
5. فحص تسجيل الدخول، إنشاء مسودة، جدولة النشر، رفع صورة WebP، وتغيير slug
   تجريبي مع Redirect 301.
6. Crawl لبيئة Staging مع Basic Auth والتأكد من عدم وجود روابط 404.
7. مقارنة sitemap في Staging مع `route-manifest.json`.
8. اعتماد مالك الموقع للهوية والمحتوى قبل جدولة أي تحويل.

## نشر Staging عام

- استخدم نطاقًا منفصلًا مثل `staging.example.com`، وليس مسارًا داخل الدومين
  الرئيسي.
- فعّل TLS وBasic Auth أو وصولًا مقيدًا بالشبكة.
- اضبط `APP_ENV=staging` و`APP_DEBUG=false` و`SESSION_SECURE_COOKIE=true`.
- أنشئ `APP_KEY` خاصًا بالبيئة، ولا تنسخ مفتاح الإنتاج.
- استخدم قاعدة منفصلة ومستخدم قاعدة بأقل صلاحيات ممكنة.
- شغّل Laravel خلف PHP-FPM، وقدّم `frontend/dist` كملفات ثابتة، ومرّر
  `/admin` و`/api` و`/storage` إلى Laravel.
- احتفظ بترويسة `X-Robots-Tag: noindex, nofollow, noarchive` حتى الإطلاق.

## خطة التحويل لاحقًا

1. خفّض TTL قبل نافذة الإطلاق بوقت كافٍ.
2. جمّد تعديل المحتوى القديم لفترة التحويل فقط.
3. خذ Snapshot نهائيًا للنسخة القديمة وقاعدة Staging.
4. نفّذ اختبارات القبول على الـcommit المرشح نفسه.
5. حوّل Reverse Proxy أو أصل الاستضافة إلى الإصدار الجديد دون تغيير أي URL.
6. راقب 404 و5xx وRedirect chains وطلبات Googlebot وسلامة Canonical وsitemap.
7. لا تحذف النسخة القديمة بعد التحويل؛ احتفظ بها خلال فترة المراقبة.

## الرجوع الفوري

1. أعد Reverse Proxy أو أصل الاستضافة إلى مجلد النسخة القديمة المحفوظ.
2. لا تعكس DNS إلى هدف جديد إذا كان الرجوع داخل نفس المنصة؛ تغيير الأصل أسرع.
3. أوقف الكتابة إلى قاعدة النظام الجديد واحتفظ بها للتحليل، ولا تحذفها.
4. أعد Snapshot الوسائط إذا كان قد حدث رفع بعد الإطلاق.
5. تحقق من الصفحة الرئيسية وعينة من الخدمات والمقالات والمناطق وsitemap.
6. أبقِ Redirects التي أصبحت منشورة موثقة؛ لا تحذفها عشوائيًا عند إعادة المحاولة.
7. سجّل سبب الرجوع والـcommit والتوقيت قبل بدء الإصلاح.

معيار نجاح الرجوع هو استعادة جميع روابط `route-manifest.json` من المرجع
القديم بلا تغيير في Canonical أو Schema أو الروابط الداخلية.
